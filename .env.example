# انسخ الملف إلى `.env` ولا ترفع `.env` لأي مستودع عام.
ONESIGNAL_APP_ID=
ONESIGNAL_REST_API_KEY=

# Google Maps Static Maps (optional, for real map preview images)
# If empty, the app falls back to a placeholder but still opens Maps when tapped.
MAPS_API_KEY=
MAPS_STATIC_API_KEY=
